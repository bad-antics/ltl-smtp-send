// ltl-smtp-send/main.ltl
//
// SMTP client — EHLO / STARTTLS / AUTH-PLAIN.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-smtp-send v0.1.0")
    io::println("SMTP client — EHLO / STARTTLS / AUTH-PLAIN.")
    0
}
