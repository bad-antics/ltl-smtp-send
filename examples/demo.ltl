// examples/demo.ltl — runnable demo for ltl_smtp_send

import std::io
use ltl_smtp_send as lib

fn main() {
    io::println("ltl_smtp_send demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}